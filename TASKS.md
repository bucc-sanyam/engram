# Tasks

> One active milestone at a time. Move finished items to `SESSION_LOG.md`.

## Active milestone
- [ ] **Deploy to Render** — `render.yaml` exists; needs env vars (Supabase URL/anon key, Gemini key) and a production smoke test. Supabase free tier covers db + auth; no extra service needed.

## Backlog
- [ ] Commit the pending work (brain label overhaul + dashboard/profile changes + context files) — working tree is dirty.
- [ ] Install the `graphify` CLI so `graphify update .` / `graphify query` work again (auto-install was blocked in-session; graph in `graphify-out/` is now stale re: ProgressCalendar, /profile, lib/text — read graph.json directly with python meanwhile).
- [ ] Consider zero-shadow label variant if the soft glow still reads as a boundary.

## Done (recent)
- [x] Plan markdown leak fix + progress calendar + /profile page + signup name (2026-07-13, see SESSION_LOG)
- [x] Brain label overlap fix + clickable labels + Space Grotesk (2026-07-13, see SESSION_LOG)
