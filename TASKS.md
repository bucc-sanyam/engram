# Tasks

> One active milestone at a time. Move finished items to `SESSION_LOG.md`.

## Active milestone
**12-Week Growth Playbook** (`GROWTH_PLAYBOOK.md`) — take Knovis from pre-revenue MVP to validated/monetizable/launched. One week at a time, in order. Beachhead = coding-interview preppers (`ICP.md`); OMTM = Day-7 return % (`OMTM.md`). Positioning is outcome-first ("Learn it once. Never forget it."), no competitor comparison.
- [x] **Week 1 — Instrument + activate the moat (2026-07-25, see SESSION_LOG).** PostHog live (5 funnel events: `landed`/`guest_started`/`recall_graded`/`streak_advanced`/`signed_in`; init `flush`-callback bug fixed; project `phc_` key required, not personal `phx_`). ICP one-pager + OMTM written. RAG diagnostic + backfill scripts ready (`scripts/diagnose-rag.mts`, `scripts/backfill-rag.mts`). 🧑 **still owed:** run `schema-rag.sql`/`schema-rag-v2.sql` in Supabase + the RAG backfill (see backlog); set the two `NEXT_PUBLIC_POSTHOG_*` vars in Vercel so PROD captures traffic.
- [x] **Week 2 — Positioning + time-to-aha (2026-07-25, see SESSION_LOG).** Outcome-first hero "Learn it once. Never forget it." across all site-wide static lines (Nav subtitle, login h1+tagline, footer blurb, layout metadata). Guided tour cut 14→6 steps with the graded-recall aha moved to step 2 → cold-load to graded report card in ~2 clicks, <60s. tsc clean, verified in preview, shipped to prod (`main` @ `06f34f1`). **Deferred (founder-copy items, not done):** surface the `/about` forgetting-curve chart as a landing section; write 3 competitor kill-sentences.
- [ ] **Week 3 — Recruit 20 real users + feedback loop (NEXT).** 🧑 recruiting posts (r/leetcode, r/cscareerquestions, prep Discords) + a private channel + personal onboarding. 🤖 build: turn on PostHog session replay (mask inputs), a minimal on-brand waitlist + feedback capture (Supabase `waitlist(id,email,goal,created_at)` insert-only RLS, `waitlist_joined` event), and a floating "Give feedback" affordance. See GROWTH_PLAYBOOK Week 3 for the full agent brief + exit gate.
- [ ] **Weeks 4–12** — validation (problem interviews + fake Pro paywall + GO/FIX gate) → monetizable loop (Stripe, share artifact, single front door) → launch (SEO engine, build-in-public, launch week, post-launch retention readout). Details in `GROWTH_PLAYBOOK.md`.

## Paused milestone (pre-growth redesign — resume after retention signal)
Redesign pass parts 1–3 shipped (homepage story real estate, Review→Recall rename, DSA/SQL/Macro content+viz — all in Done). Part 4 remains:
- [ ] **Paper Mode redesign** — replace the `!important` recolor cascade with a scoped `data-theme="paper"` token approach; structural (not just color) changes to glass cards/code blocks/grain texture on reading routes. **Partial (2026-07-21):** transition animation redone twice — wave → CSS folding tiles (rejected as unnatural) → **canvas paper-shard "snap"** (small rotating/flipping paper shards + ash particles, sweeping from the bottom-right button; `ThanosSnapCanvas.tsx`). Accent contrast fixed the right way: bright per-topic pastels are darkened at render time via theme-aware components (`src/lib/viz-theme.ts` + `src/components/AccentText.tsx`) rather than more inline-hex CSS remaps (which can't enumerate arbitrary topic colours) — applied to all 4 viz primitives + DSA question/chapter pills & eyebrows. The `!important`-cascade → token refactor itself is still open, and the same accent-component treatment should be extended to the SQL/SARFAESI/Macro hub pages (still use raw inline `style={{color: topic.color}}`). **Per GROWTH_PLAYBOOK standing rules, the craftsmanship backlog (this + cross-encoder reranking) is frozen until there's revenue + retention signal.**

## Backlog
> Code-actionable items were cleared 2026-07-26 (the two security-audit findings — see Done). What remains needs your Supabase SQL editor, or is a scoped enhancement / a recorded decision.

### 🧑 Owed by you — DB migrations (need the Supabase SQL editor; no raw-SQL/DDL access from this environment)
- [ ] **Streak repair (premium):** run `supabase/schema-streak-repair.sql` — adds `profiles.is_premium` + `guard_is_premium` trigger + `streak_repairs` table + RLS. Until run, the feature is dormant (select degrades to not-premium). To grant a user premium: `update public.profiles set is_premium = true where id = '<uuid>'` **via the service role** (the trigger blocks non-service-role changes). Env knobs (optional): `STREAK_REPAIR_MAX_GAP_DAYS` (default 1), `STREAK_REPAIR_MONTHLY_LIMIT` (default 3). **Follow-up (code, not owed):** build the "Restore your streak" UI (homepage/recall CTA → older-day recall → `redeemStreakRepair`); wire real premium provisioning (Stripe webhook flipping `is_premium` via service role).
- [ ] **`ai_graded` column:** `alter table public.quiz_sessions add column if not exists ai_graded boolean not null default false;` — until run, the AI-grading daily-cap count errors against a missing column and quiz `finish` silently falls back to heuristic grading every time (safe, but no AI grading at all). Present in both `schema.sql` and `schema-quiz-tables-only.sql`.
- [ ] **Quiz tables:** run `supabase/schema-quiz-tables-only.sql` (creates `questions`, `facts`, `quiz_sessions`, `quiz_answers`; handles policy conflicts). An already-created DB only needs `alter table public.questions add column if not exists correct_indices jsonb;`
- [ ] **RAG activation:** run `supabase/schema-rag.sql` THEN `schema-rag-v2.sql` (`vector` ext, `knowledge_chunks` + HNSW; `rag_documents`/`rag_index_pointers`/`rag_retrieval_logs` + hybrid search). Both safe to re-run; RAG stays dormant and error-free until then. Confirm whether `schema-rag.sql` was ever actually run in prod. Backfill: existing entries/answers aren't embedded retroactively — re-ingest, or run a one-off chunk+embed script over `entries.raw_text`.
- [ ] **Drop legacy `xp` column (optional):** `alter table public.profiles drop column xp;` — removed from `schema.sql` on 2026-07-16 but still in the live DB (harmless, unused, always 0).

### School track
- [x] **NCERT figure licensing — CLOSED 2026-08-03, resolved by deletion.** Chapter 2 is now 10 original drawn plates. Every page scan (`public/school/figures/**`), the extraction pipeline (`scripts/extract-book-figures.py`, `scripts/book-figures.json`) and the `plate` spec kind (`PlateSim.tsx`, `PlateSpec`, the validator's plate pass) are deleted. **No third-party artwork remains anywhere in the school track**, so there is nothing left to license. The two superseded items are struck through below for the record.
- [ ] ~~**Judge the drawn plant cell (`/learn/class-9/science/ch-02-cell`, section 1).**~~ Done — accepted, and the rest of the chapter was redrawn to match. It sits directly above the extracted book plate for comparison. Original artwork, pseudo-3-D cut-away, click-a-part-to-lift-it-out. **If it passes:** redraw the other 7 Ch2 figures the same way, delete `public/school/figures/**` and `scripts/extract-book-figures.py`, and the NCERT licensing item below goes away entirely. **If it doesn't:** the alternative is licensing, not a lighter redraw — a "tweaked" copy of NCERT's illustration is a derivative work and gives no protection.
- [x] ~~**NCERT figure licensing (decide before anything school-track ships).**~~ Superseded by the deletion above. Original wording: Ch2 now renders the book's own plates (`public/school/figures/science/ch-02/`, extracted by `scripts/extract-book-figures.py`). Every source page carries a diagonal **"© NCERT / not to be republished"** watermark baked into the page art — it is not extractable text, it lands inside some crops (visibly on Fig. 2.10), and it was deliberately NOT removed. Each plate prints a `credit` line. Three ways out, your call: (a) get written permission from NCERT for the figures; (b) commission/redraw originals in the book's illustrative register (not the flat-box style that was rejected — soft organic forms, fine leader lines, serif italic labels); (c) ship only chapters whose figures are ours. Until this is settled the plates are a **local trial**, not shippable — do not push `public/school/figures/**` to a public deploy.
- [x] ~~**Decide Fig. 2.10: drawn pair vs book scan**~~ — moot, the scans are deleted. Superseded: Section 1 currently renders `[plantCellPlate, animalCellPlate, cellTypesPlate]` — the two new drawn plates AND the book scan of the same figure. Dropping `cellTypesPlate` de-duplicates the section and removes the licensing exposure on the chapter's headline figure; the only thing lost is the bacterial cell (2.10a), which would need drawing as a third plate. One line in `ch-02-cell.ts:145`.
- [ ] **Restyle the 13 science plates' artwork (the visual half of "convert everything to this format").** 🤖 **Briefs are written — `prompts/school-chapters/targets-existing-chapters.md` blocks 1 and 2 are ready to run per plate**, on top of `prompts/school-chapters/author-a-chapter.md`. Interaction is done — 8 plates lift + tag, 5 comparison plates stay on camera, maths stays flat by design. What's left is the ch-02-cell-plate treatment applied per plate: a `gleam()` highlight on every solid, wall/membrane layers, nuclei and interior texture. **The bar is now measured, not vibes** (from Ch2's ten plates): ≥5 parts, ≥1.0 detail layers per part, ≥70% of parts carrying at least one. Worst offenders: the neuron (0.2 layers/part, 17% covered — a bare outline) and the separating funnel (0.1, 14%). **Do NOT retry the mechanical "make everything organic" pass** — it was tried on 2026-08-03 and reverted: boxy is often anatomically correct (collenchyma and sclerenchyma cells really are polygonal), and rounding bodies leaves detail layers stranded outside them. Order: ch-03 tissues (7), then ch-05 mixtures (6). Verify each with `npx tsx scripts/render-figures.mts .figures --lift <partId>`.
- [ ] **Convert the rest of both books — 14 new chapters + finishing maths 6.** 🤖 **Programme written: `prompts/school-chapters/convert-the-books.md`.** Real inventory taken from the PDFs on disk: Exploration has 13 chapters (ch1 is a preface — skip; 2/3/5 done; **9 to author**), Ganita Manjari Part I has 8 (1/3 done, **6 is half-done and mis-titled — the printed title is "Perimeter and Area"**; 5 to author). The file carries the running order (maths-6 Area first, then the four physics chapters as a batch since they share a diagram register, then biology, chemistry, earth systems, then the flat maths ones), distinct accent hexes for every remaining chapter, and the two things a generic prompt gets wrong: **mapping a printed chapter's 3–10 sections onto the validator's hard 5–7**, and **choosing the diagram register per subject** — a free-body diagram or a velocity–time graph is a construction, so it stays flat and on `camera` like a maths figure, while the pulley beside it is a specimen and lifts. One chapter per model run, no batching.
- [ ] **Roll plates out to the other chapters (only after the above).** Pattern is established: add crop boxes to `scripts/book-figures.json`, run `python3 scripts/extract-book-figures.py <book> <chapter> --contact`, author a `ch-*-plates.ts` with `focus` fractions read off a grid overlay, point the chapter's `figures:`/`sim:` at them. Then **delete the superseded hand-drawn plates** — `src/lib/cbse/class9/science/figures/ch-02.ts` (1299 lines) is already unreferenced and only kept while this is a trial.

### Enhancements (code, when prioritized)
- [ ] **More book figures:** 31 plates cover the load-bearing ones; the printed chapters have plenty more (Ch2 osmosis + meiosis, Ch3 joints + growth rings, Ch5 crystallisation + centrifuge, Maths reflections + area-by-dissection). Pattern is established — add a `FigureSpec` to the chapter's `figures/ch-*.ts` and list it in `Section.figures`. 🤖 **Two are briefed** — `targets-existing-chapters.md` block 3 (maths ch-01 reflections, the one section with no figure) and block 4 (maths ch-03 place-value/zero, the most visual idea in the chapter and currently unillustrated).
- [ ] **Area Content for ch-06**: CBSE Class 9 maths ch-06 has been retitled to just "Perimeter". The "Area" content is currently missing and needs to be authored. 🤖 **Brief written — `prompts/school-chapters/targets-existing-chapters.md` block 5.** Note the constraint it encodes: the chapter is at 5 sections and the validator caps at 7, so Area gets exactly two, and the 50-question bank has to be rebalanced (not grown) to keep the fixed mcq 20 / tf 10 / multi 8 / quickfire 6 / open 6 mix.
- [ ] **Notes cross-device sync:** notes are localStorage-only (per-browser). Add a Supabase `notes` table (id, user_id, parent_id, title, body, position, timestamps) + RLS, and branch `src/lib/notes.ts` demo(localStorage)/real(Supabase) like `data.ts`.
- [ ] **Question-bank replenishment for legacy topics:** topics ingested before the bank existed get only the synthesized fallback question — a one-off backfill (1 Gemini call/topic) or regenerate-on-empty.
- [ ] **Blog long-form grounded in RAG:** the `body` is generated at ingest from the entry's own text only — could enrich `generateBlogStructure()`/`extractKnowledge()` with `retrieveContext` for cross-entry grounding (still ingest-time, renders stay AI-free).

### Decisions (recorded — no action unless revisited)
- **Cross-encoder reranking:** chose hybrid-only (option c). Local ONNX adds a ~25-90MB WASM dep + cold start; Gemini reranking breaks the 1-call/ingest budget. Revisit if hybrid recall/precision proves insufficient at scale.
- **DB scaling:** stay on Supabase; at ~500MB upgrade to Pro ($25/mo, 8GB) rather than migrate to TiDB (no auth/RLS, both load-bearing). Watch 768-dim HNSW vector storage; consider capping chunks/user or pruning old `source='answer'` chunks.

## Done (recent)
- [x] **Lift no longer breaks shapes; diagrams get their own column (2026-08-03, see SESSION_LOG).** Three owner-reported fixes. (1) The rectangular lift clip sliced any part bigger than its own focus box (mitochondrion inner membrane, chloroplast grana, membrane lipid heads all lifted as hard rectangles) — replaced by whole-subpath selection (`liftSubset`/`layerSubset` + a path measurer `subpaths()` in `draw.ts`, shared with `render-figures.mts`). (2) `Section.figures[]` moved into the sticky rail so all diagrams sit right of the prose. **Rebalanced on owner feedback the same day** — the rail is now a fixed 640px (exactly what the widest plate needs) with the prose on `1fr`, the route fills the window at `max-w-[1600px]` instead of `max-w-6xl`, and two columns wait for `min-[1400px]`; text went from 440px to 678px at a 1440 window. `Nav` matches the wider container on chapter routes above 1400 only. (3) The microscope's objective was a `blob` mushroom cap; redrawn as a `roundRect` nosepiece + tapered barrel with a band and a lens.
- [x] **Textbook plates — click-to-magnify figures across all six school chapters (2026-08-02, see SESSION_LOG).** New `figure` sim kind + `FigureSim.tsx`: the whole specimen drawn at once with leader-line labels, click any component to magnify into it (CSS transform off an authored `focus` box; Zoom out / Esc / click-away). `Section.figures[]` renders plates inline at reading width. **30 plates authored** (Ch2 9 · Ch3 7 · Ch5 6 · Maths 8) in `src/lib/cbse/class9/*/figures/`, with `src/lib/sim/draw.ts` path helpers and `scripts/render-figures.mts` to draw + audit them. **`three.js` removed from `/learn`** (3-D cell replaced; `Anatomy3DSim`/`geometry.ts`/`organic.ts` deleted) — spec Rule 3 now holds, and the dead code flagged on 2026-08-01 is cleared. tsc + validate-cbse + build clean; magnification verified in a real browser.
- [x] **Streak repair API — PREMIUM (2026-07-27, see SESSION_LOG).** New `GET/POST /api/streak/repair`: maintain a streak a missed day would break by attempting an older-day recall. `POST` requires a completed graded session (proof-of-effort), backfills the missed day into `streak_repairs`, and recomputes the streak from `reviews`∪`streak_repairs` (ungameable). Pure logic in `src/lib/streak.ts` (9 unit tests pass, incl. the ordering case where the make-up recall already advanced `last_active`). Adds `profiles.is_premium` (+ service-role-only guard trigger) + `streak_repairs` table (`schema-streak-repair.sql`), client helpers, types. tsc + build clean. 🧑 owed: run the migration + grant premium; UI is a follow-up.
- [x] **Blog/brain visibility + experts preview (2026-07-27, see SESSION_LOG).** `/blogs` and `/brain` now share `getBlogTopicLibrary()`: only user-added (`entry_topics`) or completed-story (`story_sections.status="learned"`) topics render; hidden seeded topics cannot leak through brain edges/search/connections. "Learn from our experts" previews one story with a below-row expansion arrow. Demo retains all 200 topics; tsc + 412-page build + prerender assertion clean.
- [x] **Diagram overflow fix + hardened auto-blog viz generation (2026-07-27, see SESSION_LOG).** SVG viz primitives (`ArrayViz`/`TreeViz`/`FlowViz`) were shrinking wide diagrams into illegible smears (worst tree 1671px → 27px tall on mobile). Added a **legibility floor** (`minWidth: naturalW*0.72` inside `overflow-x-auto` → shrink-then-scroll, never squash); rewrote `TreeViz` with multi-line label wrapping (worst tree 1671→714px); fixed 9 `$\rightarrow$`→`→` mangled array notes. Future-proofed ingest: `stripInvalidVizBlocks()` now normalizes (drops over-`VIZ_BUDGETS` / sanitizes `$…$` + control chars / re-emits compact) and `BLOG_BODY_SPEC` demands small plain-text diagrams. tsc + build + validate-viz clean, verified in browser. **Known follow-up (flagged):** body math (not diagrams) has the same `\r`/`\t`/`\b`/`\f` template-literal escape corruption (`\text{}`→"text", `\times`, `\frac`, `\beta`…) across DSA bodies — broad content sweep, deferred.
- [x] **Homepage "Revision needed" + voice answers in recall (2026-07-27, see SESSION_LOG).** (1) Replaced the homepage "Recent learnings" section with a report-card-driven "Revision needed" list — topics whose latest recall score is `< 4/5`, ranked weakest-first, each linking to `/blogs/[id]` to re-read (`RevisionRow`/`scoreColor` in `page.tsx`; removed the unused `entries` state). (2) `MicButton` (`src/components/MicButton.tsx`, Web Speech API, zero deps) on open/quickfire recall textareas + a bottom-of-page spoken-English tip. tsc + build clean.
- [x] **Three user-reported fixes (2026-07-26, see SESSION_LOG).** (1) Story-recall resurfacing: `/api/plan` no longer rebuilds the cached day to swap in over-cap due topics after you finish the shown ones — added `DailyPlan.built_at`, rebuild only for topics due after build (`.gt("next_review_at", builtAt)`); finishing a story's recall now stays done for the day. (2) Medium/paywall 403 on link ingest: `fetchReadable` now sends a real desktop-Chrome UA + browser headers (was `KnovisBot/1.0`), and 401/403/429 returns a "paste the text instead" hint. (3) Empty new account: the homepage "Today's recall" card is hidden when the plan has zero items. tsc + build clean.
- [x] **Backlog security fixes — the 2026-07-18 audit's two low-severity findings (2026-07-26, see SESSION_LOG).** (1) Three API 500s no longer leak raw Postgres error text (`entryErr.message` / `error.message` / `docsErr.message` in ingest / quiz-answer / rag-reindex) — the real error is `console.error`-logged server-side and a generic message is returned. (2) `gradeSession()` + `gradeCommunicationSession()` prompts now frame the learner's answer as UNTRUSTED (never an instruction), and every grader free-text field (feedback, correct_answer, comm tips + `improved_answer`, summary, strengths, focus) is run through `sanitizeField()` before storage — matching the `extractKnowledge()` pattern. tsc + build clean.
- [x] **The Communication Lab + answer judge — Phases 1 & 2 (2026-07-26, see SESSION_LOG).** New static story series `src/lib/english-communication/` — now **8 chapters / 25 sections + 18 viz diagrams** (Foundations, Structuring Your Message, Delivery, Everyday Scenarios, Meetings & Presentations, Written Communication, High-Stakes Conversations, Remote & Cross-Cultural) on the proven story-series pattern, category **"Communication"**. Novel bit: `gradeSession(items, mode)` (gemini.ts) grades `open` scenario answers on **Content/Structure/Delivery** + `tips` + an `improved_answer` rewrite (1 Gemini call/finish; `heuristicCommunicationScore` fallback). `ReportItem.comm` (`CommGrade`, TS-only, no migration) → 3 `CommMeter` bars in `ReportCardView`. Verified in demo (topic `d15` + `demoCommGrade`): report shows C 4/5 · S 5/5 · D 4/5 + tips. tsc + build clean, all 13 SSG pages.


- [x] **Blog/recall/paper-mode fixes (2026-07-21):** inline LaTeX-ish math rendering in `Markdown.tsx` (`$O(1)$`/`$\text{…}$`/sup-sub/greek, with a currency guard so `$1 billion` stays currency); per-series topic-wise report cards in `/recall` (each series group = its own session + inline report on submit; demo sessions now a Map keyed by id); collapsible "Learn from our experts" dropdown on `/blogs` + a "Want a dedicated story?" write-to-us modal (mailto); paper-mode transition changed from a reveal wave to folding paper tiles; paper-mode near-white contrast leaks fixed (inline rgba titles + viz SVG fills). See SESSION_LOG.
- [x] **DSA: per-question diagrams + hellointerview-style rewrite for all 150 questions — COMPLETE 2026-07-21.** Every one of the 150 DSA question bodies (across all 18 `src/lib/dsa/topics/*.ts` chapter files) rewritten to a Signal / Brute-force / Optimal-approach / Diagram / Complexity / Thread structure, each with its own `viz:*` diagram (was 44/150 before, now 150/150). Done sequentially, chapter by chapter (not parallel agents, per explicit user request — this content is the site's main selling point). Also normalized the four chapters still on the older ASCII-art/Python-fence template (`two-pointers.ts`, `binary-search.ts`, `dp-1d.ts`, `dp-2d.ts`) to the prose-only house style in the same pass. Mid-rollout, a separate concurrent process added hardcoded `questions` (quiz) arrays to already-rewritten chapters — those were detected and preserved untouched throughout (verified via `grep -c 'kind:'` before/after each affected chapter). Verified clean via `npx tsx scripts/validate-viz.mts` (all 150 diagrams parse), `npx tsc --noEmit`, and a full `npm run build` (all 150 question pages + 18 chapter pages render as static HTML with no errors). Full per-question tracking in [DSA_HELLOINTERVIEW_CHECKLIST.md](DSA_HELLOINTERVIEW_CHECKLIST.md) (all boxes now checked).
- [x] **DSA Pattern Atlas — complete** — static blog series covering the whole NeetCode 150 roadmap: 18 chapter blogs + 150 question blogs with one linear prev/next reading path. Content in `src/lib/dsa/topics/*.ts`, registry+nav helpers in `src/lib/dsa/index.ts`, routes `/blogs/dsa[/topic[/slug]]` (all SSG). Question list/difficulties verified against live neetcode.io (28E/101M/21H exact match); prereq/unlock DAG validated. Zero AI, zero DB. (2026-07-19, see SESSION_LOG)
- [x] **Security audit + fixes: SSRF DNS-rebinding, quiz Gemini-quota abuse, curl-hardening** — full server-side audit (API auth, RLS, secrets, deps) with findings ranked by severity; fixed the two most serious: (1) SSRF via DNS rebinding in link ingestion, closed with a DNS-pinning `undici` dispatcher (`isBlockedIp`/`ssrfSafeDispatcher` in `src/app/api/ingest/route.ts`) verified against real `*.nip.io` rebinding domains; (2) unbounded Gemini-quota abuse via quiz sessions, fixed with a new independent `QUIZ_AI_DAILY_LIMIT`=1/day cap (ingest's own cap lowered 20→9), plus a new "come back at X" popup on `/add` when the ingest cap is hit (timezone-correct now, was using server-local time). Also tightened `PUBLIC_PATHS` middleware matching from prefix to exact/segment match. (2026-07-18, see SESSION_LOG)
- [x] **XP feature removed entirely** — `xpForReview()` deleted, `awardXp()` renamed to `advanceStreak()` (streak logic only, no xp param), `xp` field dropped from `Profile`/`ReportCard`/`IngestResult` types and every API response/demo report builder, the visible `+{xp} XP` line removed from `/add`, `profiles.xp` column dropped from `supabase/schema.sql` (fresh-install reference only — see backlog for the live-DB column). (2026-07-16, see SESSION_LOG)
- [x] **Ingest guardrails** — gibberish/spam rejection before the Gemini call (`src/lib/guardrails.ts`, dependency-free heuristics, tested against a 10-case suite); every field Gemini's extraction returns is now validated/clamped before DB insert (category allowlist, length caps); prompt-injection hardening in the extraction prompt (untrusted-data framing, relevant since link ingest feeds third-party page text into it); fixed a real `javascript:`/`data:` URI XSS gap in `Markdown.tsx`'s link renderer (verified live). **Same-day follow-up fix:** the guardrail was rejecting real articles (ASCII-divider false positive + a non-scale-invariant distinct-char check that broke on any prose past ~2 sentences) — both fixed, see SESSION_LOG. (2026-07-16, see SESSION_LOG)
- [x] **RAG pipeline hardening (v2)** — document registry with content-hash dedup (fixes duplicate-ingest bug), alias-based per-user index versioning + self-serve `/api/rag/reindex`, hybrid vector+BM25(tsvector) search with RRF fusion, and an observability layer (chunk-level retrieval logs, quality metrics, index-version/quiz-score correlation). Reranking evaluated and deferred. Requires `schema-rag-v2.sql`. (2026-07-15, see SESSION_LOG)
- [x] **Rebrand: Engramia → Knovis** — new name (knowledge + vision, echoes "novice") across the whole app: Nav wordmark/tagline, Footer, login page, layout metadata, guided tour copy, profile page, About page origin story rewritten around the Knovis/novice theme, package.json, README. Guest cookie (`engramia_guest`→`knovis_guest`) and all localStorage/sessionStorage keys (tour, demo state, notes) renamed to a `knovis.*` prefix — old Engramia-era guest sessions/local data reset silently, no migration. (2026-07-15, see SESSION_LOG)
- [x] **Tour interactivity + review-demo step + add-page demo gate** — spotlighted features (calendar, brain search/canvas) are now genuinely clickable during the tour (overlay `pointer-events:none` fix); link navigation blocked globally while touring so clicks can't route away; new self-contained "Try a review" mock-quiz step (14 steps total); `/add` shows an honest "This is a demo" popup instead of a fake simulated success. (2026-07-15, see SESSION_LOG)
- [x] **Guided feature tour** — 13-step spotlight walkthrough of every feature (dashboard, brain, blogs, add) with translucent on-brand step cards; auto-runs on a browser's first visit (then hands off to /login) and replays on every "Continue as guest" click. `src/lib/tour.ts` + `src/components/TutorialTour.tsx` + `data-tour` anchors. (2026-07-15, see SESSION_LOG)
- [x] **Attempted-tracking bug + security review + Gemini fallback + RAG + DB decision** — fixed local-day vs UTC done-tracking and the `?topic=` reroute (Suspense + `useSearchParams`); SSRF guard + daily ingest cap + JSON guards + auth-callback error redirect; Gemini model fallback chain; full RAG pipeline (chunk/embed/retrieve, `schema-rag.sql`, ingest grounding + answer indexing, best-effort); decided to stay on Supabase over TiDB. Demo progress now persists to localStorage. (2026-07-15, see SESSION_LOG)
- [x] **Quiz-kind overhaul + day reports + blog Q&A + streak bar + footer/About** — flashcards fully removed; truefalse & multi-select question kinds added end-to-end; report card lists every question with full Q&A; calendar days clickable → day report modal; blog pages show their question bank with answers; streak-milestone progress bar on Momentum card; engram meaning tagline; global footer + quirky /about. (2026-07-14, see SESSION_LOG)
- [x] **Rebranding, Mastery/XP Removal, True/False Statements & Review History Details** — Renamed Engram to Engramia globally, disabled XP/mastery metrics, replaced flashcards with graded True/False statement cards, show detailed prompt/answer/stored response breakdowns for already-done items, and change Start button to "See report" when daily plan is completed. (2026-07-13, see SESSION_LOG)
- [x] **Review page bug fixes + /blogs page** — (1) done questions show "Already reviewed today" card instead of re-asking; (2) full-plan session now includes all items (done ones auto-skip) so report shows after all items not just 1; (3) stats "Due today" → "In plan" for consistency; (4) new `/blogs` library page + nav link, "Back to your brain" removed from blog pages. (2026-07-13, see SESSION_LOG)
- [x] **Guest mode + plan polish** — fixed guest/demo loop on production (cookie + proxy), plan done-tracking (green dots, done-last, expandable), review error card instead of blank page, graph-card pills → blog pages. (2026-07-13, see SESSION_LOG)
- [x] **AI-call minimisation redesign** — ingest-time question bank (incl. MCQs) + facts, per-answer DB saves, single batch-grade report card, zero-AI plan narrative, fact of the day. (2026-07-13, see SESSION_LOG)
- [x] **Install graphify CLI** — `pipx install graphifyy` (v0.9.14) at `/Users/bucc/.local/bin`; graph regenerated (384 nodes). (2026-07-13)
- [x] **Personal notes + blog source** — `/notes` (localStorage, subnotes, markdown Write/Read), dashboard "Personal notes" section, blog source attribution (url vs "Text input"). (2026-07-13, see SESSION_LOG)
- [x] **Dashboard/brain polish** — "The thread today" rich insight, clickable plan rows → single-task review, topic blog pages (Fraunces serif), summarised brain card + Read-full, smaller 3D labels. (2026-07-13, see SESSION_LOG)
- [x] **Deploy to Vercel** — migrated from Render. Removed `render.yaml`, connected Vercel CLI, deployed to production. (2026-07-13, see SESSION_LOG)
- [x] **Fix Nav Auth UI** — fixed bug in `Nav.tsx` where the "Sign in" button was hidden for unauthenticated users when a database was connected. (2026-07-13, see SESSION_LOG)
- [x] Plan markdown leak fix + progress calendar + /profile page + signup name (2026-07-13, see SESSION_LOG)
- [x] Brain label overlap fix + clickable labels + Space Grotesk (2026-07-13, see SESSION_LOG)
